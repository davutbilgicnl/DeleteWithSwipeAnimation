import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const SwipeDeleteAnimation = () => {
  return (
    <View>
      <Text>SwipeDeleteAnimation</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default SwipeDeleteAnimation;
